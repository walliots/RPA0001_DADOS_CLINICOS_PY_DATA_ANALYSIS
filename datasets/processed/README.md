# datasets/processed

Dados após tratamento: remoção de duplicatas, preenchimento de valores ausentes (moda para categóricas, mediana para numéricas) e resultado do inner join entre `dados_clinicos`, `dados_pacientes` e `estado_regiao`.
